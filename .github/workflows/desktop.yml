name: Build Desktop Apps

on:
  push:
    branches: [main]
    paths:
      - 'windows/**'
      - '.github/workflows/desktop.yml'
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: windows
        run: npm install

      - name: Build Electron app (dev build)
        if: "!startsWith(github.ref, 'refs/tags/v')"
        working-directory: windows
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist

      - name: Build & Publish Electron app (release)
        if: startsWith(github.ref, 'refs/tags/v')
        working-directory: windows
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run publish

      - name: Upload NSIS installer (dev builds only)
        if: "!startsWith(github.ref, 'refs/tags/v')"
        uses: actions/upload-artifact@v4
        with:
          name: Voxa-Windows-Setup-${{ github.sha }}
          path: windows/dist/*.exe
          retention-days: 30
          if-no-files-found: warn

      - name: Upload portable exe (dev builds only)
        if: "!startsWith(github.ref, 'refs/tags/v')"
        uses: actions/upload-artifact@v4
        with:
          name: Voxa-Windows-Portable-${{ github.sha }}
          path: windows/dist/*Portable*.exe
          retention-days: 30
          if-no-files-found: warn
