name: Build iOS App

on:
  push:
    branches: [main]
    paths:
      - 'ios/**'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "iOS build pipeline — configure when Xcode project is added."
